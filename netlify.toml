[build]
  publish = "frontend"
  functions = "netlify/functions"

# En producción y en netlify dev, el frontend llama a /api/*
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
